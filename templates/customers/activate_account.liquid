<section class="section-padding">
  <div class="page-width" style="max-width: 22rem;">
    <h1>{{ 'customer.activate_account.title' | t }}</h1>
    <p style="color: var(--color-muted);">{{ 'customer.activate_account.subtext' | t }}</p>
    {% form 'activate_customer_password' %}
      {{ form.errors | default_errors }}
      <div class="field">
        <label for="CustomerPassword">{{ 'customer.activate_account.password' | t }}</label>
        <input type="password" name="customer[password]" id="CustomerPassword" autocomplete="new-password" required>
      </div>
      <div class="field">
        <label for="CustomerPasswordConfirmation">{{ 'customer.activate_account.password_confirm' | t }}</label>
        <input type="password" name="customer[password_confirmation]" id="CustomerPasswordConfirmation" autocomplete="new-password" required>
      </div>
      <button type="submit" class="btn" style="width:100%;">{{ 'customer.activate_account.submit' | t }}</button>
      <button type="submit" name="decline" class="btn btn--secondary" style="width:100%; margin-top:0.5rem;">{{ 'customer.activate_account.cancel' | t }}</button>
    {% endform %}
  </div>
</section>
